using System;

namespace CQRS.Projections.Service
{
    class Program
    {
        static void Main(string[] args)
        {
            // TODO: Write an example service to run a separate service, using a queue and running projection handlers from this queue. 
            // This would remove the projection event processing being part of the CQRS code, having a separate microservice for that.
            Console.WriteLine("Hello World!");
        }
    }
}
